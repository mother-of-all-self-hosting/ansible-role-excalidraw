# SPDX-FileCopyrightText: 2026 Slavi Pantaleev
# SPDX-FileCopyrightText: 2026 spatterlight
#
# SPDX-License-Identifier: AGPL-3.0-or-later

---
name: molecule

on:  # yamllint disable-line rule:truthy
  push:
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "CHANGELOG.md"
      - "LICENSE"
      - "LICENSES/**"
      - "REUSE.toml"
  pull_request:
    paths-ignore:
      - "docs/**"
      - "README.md"
      - "CHANGELOG.md"
      - "LICENSE"
      - "LICENSES/**"
      - "REUSE.toml"
  workflow_dispatch:

jobs:
  molecule:
    name: Run Molecule scenario '${{ matrix.scenario }}' on ${{ matrix.distro }}
    runs-on: ubuntu-latest

    # Same-repository pull requests already run via the push event;
    # run pull_request jobs only for pull requests from forks.
    if: >-
      github.event_name != 'pull_request'
      || github.event.pull_request.head.repo.full_name != github.repository

    strategy:
      matrix:
        distro:
          - ubuntu2604
          - debian13
        scenario:
          - default
          - default-selfbuild
        # The self-build scenario compiles Excalidraw with Node inside a
        # container and serves the result from another one. Neither step cares
        # which distribution the Docker daemon happens to run on, so building it
        # a second time tests nothing the first run has not.
        exclude:
          - distro: debian13
            scenario: default-selfbuild
      fail-fast: false

    env:
      MOLECULE_DISTRO: ${{ matrix.distro }}
      PY_COLORS: "1"
      ANSIBLE_FORCE_COLOR: "1"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.0
        with:
          path: excalidraw

      - name: Set up Python
        uses: actions/setup-python@v6.3.0
        with:
          python-version-file: excalidraw/.python-version
          cache: pip
          cache-dependency-path: excalidraw/molecule/requirements.txt

      - name: Install Python dependencies
        working-directory: excalidraw
        run: pip install -r molecule/requirements.txt

      - name: Cache Ansible collections
        uses: actions/cache@v6.1.0
        with:
          path: ~/.ansible/collections
          key: ansible-collections-${{ hashFiles('excalidraw/molecule/*/requirements.yml') }}

      - name: Run Molecule
        working-directory: excalidraw
        run: molecule test --scenario-name ${{ matrix.scenario }}
